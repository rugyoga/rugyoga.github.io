My simple hack to get a crude G-meter for my car using `acceleration`
(which returns the x, y, z components with the effect of gravity removed).

n.b. `accelerationIncludingGravity` includes the effect of gravity.
